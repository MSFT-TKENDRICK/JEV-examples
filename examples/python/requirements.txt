# The LangChain integration for TypeSafe is Python-only and pre-1.0.
# Pin it: 0.0.1a3 already has one breaking change behind it.
langchain-typesafe[experimental]==0.0.1a3
langchain>=1.0.0

# langchain_harness.py builds real agents around the TypeSafe middleware, so it
# needs a generative provider too. Swap this for whichever provider you use;
# if you change it, change the `openai:` model strings in the example to match.
langchain-openai>=0.2.0
